fix(mobile): 彻底修复弹层黑影闪动——补背景滚动锁定 + svh 高度

复现（390x844 移动仿真，实测）：详情打开后页面滚到 scrollY=400，在遮罩区域触摸上滑
→ 整页被拖动到 scrollY=0，半透明遮罩后的内容整页移动 = 老板看到的“黑影闪动/抖动”。
“时有时无”是因为它取决于点按瞬间手指是否有位移、以及背景是否还有可滚余量。

修复：
1) useDialogA11y 增加引用计数式背景滚动锁定（position:fixed + top:-scrollY，
   兼容 iOS 橡皮筋；桌面端补偿滚动条宽度；解锁恢复原滚动位置；多层弹窗只锁一次）
2) 弹层最大高度 vh → svh（新增 .sheet-max/.sheet-max-form/.sheet-max-date）
   地址栏收放时高度恒定，不再抽动
3) 经典版三处弹层一并去掉 backdrop-blur-sm（同类隐患）

回归用例：E2E 新增 G12（5 条）——CDP 真实触摸滑动、背景锁定状态、抽屉内滚动仍可用、
关闭后滚动位置恢复、构建产物 CSS 必须命中 svh 规则

验证：typecheck 0 / 262 断言全过 / lint 0 / build 0 告警 84.51 kB gzip / E2E 80-80