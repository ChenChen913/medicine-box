# 复制本文件为 .env.local（本地开发用），把下面的值换成你自己的 Supabase 项目信息
# 获取位置：Supabase 控制台 → Project Settings → API
VITE_SUPABASE_URL=https://xxxxxxxxxxxx.supabase.co
VITE_SUPABASE_ANON_KEY=eyJhbGciOiJIUzI1NiIsInR5cCI6IkpXVCJ9...

# 说明：
# 1. 这两个变量是「公开可见」的（会打进前端产物），Supabase 的 anon key 设计上就是允许前端使用的，
#    真正的安全靠数据库 RLS 策略，不需要保密 service_role key。
# 2. 线上 GitHub Pages 构建走 GitHub 仓库 Secrets，同名变量：VITE_SUPABASE_URL / VITE_SUPABASE_ANON_KEY
# 3. 如果这两个变量都没填，应用会自动降级为「浏览器 localStorage 存储」，功能照常，只是换设备不同步。
