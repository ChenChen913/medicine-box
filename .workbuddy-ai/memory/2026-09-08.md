## 2026-09-08

- 完成「家庭智慧药箱」项目从本地 Express/JSON 后端迁移为 Supabase + localStorage 双后端，使其可部署为纯静态站点。
- 配置 GitHub Actions 工作流（`.github/workflows/deploy.yml`），实现 push → build → Pages 自动部署。
- 推送到仓库 https://github.com/ChenChen913/smart-medicine-box，并开启 GitHub Pages（Actions 源）。
- 首次部署构建成功，站点已上线：https://chenchen913.github.io/smart-medicine-box/
- 待办：用户需要手动注册 Supabase、执行 `supabase/schema.sql` 建表，然后在仓库 Settings → Secrets 配置 `VITE_SUPABASE_URL` 和 `VITE_SUPABASE_ANON_KEY`。
